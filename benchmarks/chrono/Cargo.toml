[package]
name = "roc-time-chrono-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "=0.4.45", default-features = false, features = ["std"] }

[profile.release]
codegen-units = 1
